<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="op.mobile.app.dev.kaths4.travelling">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Viaje">
        <activity android:name=".ResultActivity"></activity>
        <activity
            android:name=".TranslationActivity"
            android:label="@string/translation" />
        <activity
            android:name=".AustraliaPhrasesActivity"
            android:label="@string/australia" />
        <activity
            android:name=".NewZealandPhrasesActivity"
            android:label="@string/new_zealand" />
        <activity
            android:name=".ColombiaPhrasesActivity"
            android:label="@string/colombia" />
        <activity
            android:name=".BrazilPhrasesActivity"
            android:label="@string/brazil" />
        <activity
            android:name=".UsPhrasesActivity"
            android:label="@string/us" />
        <activity
            android:name=".CanadaPhrasesActivity"
            android:label="@string/canada" />
        <activity
            android:name=".UnitedKingdomPhrasesActivity"
            android:label="@string/uk" />
        <activity
            android:name=".ItalyPhrasesActivity"
            android:label="@string/italy" />
        <activity
            android:name=".SingaporePhrasesActivity"
            android:label="@string/singapore" />
        <activity
            android:name=".IndiaPhrasesActivity"
            android:label="@string/india" />
        <activity
            android:name=".MauritiusPhrasesActivity"
            android:label="@string/mauritius" />
        <activity
            android:name=".SouthAfricaPhrasesActivity"
            android:label="@string/south_africa" />
        <activity
            android:name=".QuizDetailActivity"
            android:label="@string/quiz" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.map.MapsActivity"
            android:label="@string/touristPlaces" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/Theme.NoActionbar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.NoActionbar" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Theme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>